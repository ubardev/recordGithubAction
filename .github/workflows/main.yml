name: helloGithubAction

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    # run 두 실제 작동하는 코드를 넣어야 합니다.
    - uses: actions/checkout@v2
    - name: 파이썬 버전 출력!!
      run: python -V
